<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ro.kepler.kepres.app.dao.DaoTestObject">

	<select id="read" resultType="Diacritice">
		select * from DIACRITICE where ID = #{id}
	</select>
	
	<update id="update" parameterType="Diacritice">
		update DIACRITICE
		set DIACRITICE = #{record.diacritice} 
		where ID = #{record.id}
   </update>
   
   	<insert id="create" useGeneratedKeys="true" >
		<selectKey keyProperty="record.id" resultType="int" order="BEFORE">
			select SEQ_DIACRITICE.NEXTVAL from DUAL
		</selectKey>
		insert into DIACRITICE(ID, DIACRITICE)
		values (#{record.id}, #{record.diacritice})
   </insert>

</mapper>